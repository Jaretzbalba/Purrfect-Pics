<%- include('partials/header_nav') -%>
<body class="body-bg container min-vh-100 text-color d-flex flex-column">
  <div class="card row mt-5 post-card m-auto">      
    <img class="img-fluid p-0 m-0 rounded-top" src="<%= post.image%>" />
    <div class="col-12">
      <div class="row p-3">
        <form
          class="col-4"
          action="/post/likePost/<%= post.id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-4 text-center"><%= post.likes %> Likes</h3>
        <%if(post.user == loggedUser.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col-4 text-end"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="mt-2">
      <a class="user text-decoration-none text-black" href="/profile/<%=post.user%>"><%=post.userName%></a> <%=post.caption%>
    </div>

    <span class="mt-3 text-end post-created"><%=moment.utc(post.createdAt).fromNow()%></span>

    <hr class="my-3" />
    
    <div>
      <h3>Comments</h3>
      <form action="/comment/createComment/<%=post._id%>" method="POST">
        <div class="mb-3">
            <input type="text" class="form-control" id="comment" name="comment" placeholder="Add a comment...">
        </div>
        <button type="submit" class="btn btn-primary">Post</button>
      </form>
    </div>
    <ul>
      <% for(var i=0; i<comments.length; i++) {%>
        <li class="col-12 list-unstyled mt-3 comment-card p-2">
          <div class="comment-section col-12">  
            <a class="user text-decoration-none text-black" href="/profile/<%=comments[i].userId%>"><%=comments[i].userName%></a> <%=comments[i].comment%> 
          </div>
          <div class="comment-info d-flex justify-content-end align-items-end">
            <span><%=moment.utc(comments[i].createdAt).fromNow()%></span> 
            <!-- If the commentors id == logged in user id, add delete button-->
            <%if(comments[i].userId == loggedUser.id){ %>
            <form
              action="/comment/deleteComment/<%=post._id %>/<%=comments[i]._id %>?_method=DELETE"
              method="POST"
              class="text-end"
            >
              <button class="btn btn-primary fa fa-trash ms-2" type="submit" style="--bs-btn-padding-y: .25rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size: .75rem;"></button>
            </form>
            <%}%>
          </div>
        </li>
      <% } %>
    </ul>
  </div>
</body>

<%- include('partials/footer') -%>
